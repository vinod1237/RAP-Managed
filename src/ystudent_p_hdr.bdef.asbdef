projection;
strict ( 2 );
use draft;

define behavior for Ystudent_P_hdr //alias <alias_name>
{
  use create;
  use update;
  use delete;

  use action Activate;
  use action Discard;
  use action Edit;
  use action Resume;
  use action Prepare;

  use association _Attachements { create; with draft; }
}

define behavior for Ystudent_P_att //alias <alias_name>
{
  use update;
  use delete;

  use association _Student { with draft; }
}